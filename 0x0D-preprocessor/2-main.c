#include <stdlib.h>
#include <stdio.h>
/**
 * main - print the name of the file it was compile from
 * followed by a new line
 * Return: 0 if succes
 */
int main(void)
{
printf (%\n, _FILE_);
return (0);
}
