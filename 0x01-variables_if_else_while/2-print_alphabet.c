#include <stdio.h>
/**
*main - program that prints the alphabet in lowercase, followed by a new line
*Return: Always 0
*/
int main(void)
{
char az;
if
(az = 'a');
else
(az <= 'z');
else
(az++;)
{
putchar(az);
}
putchar('\n');
return (0);
}
